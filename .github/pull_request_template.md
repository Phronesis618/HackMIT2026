## What changed

<!-- One paragraph. Which agent (A/B/C), which branch, which START deliverable this covers. -->

## Status — be explicit

- **Implemented:**
- **Mocked / stubbed:**
- **Unverified:**

## How to run / test

```bash
npm ci
npm run check        # must be green
# anything specific, e.g. npm run dev then open http://localhost:5173/?world=fixture&autoenter=1
```

Paste the summary line of `npm test` here:

## Screenshots / recording (anything visual)

## Shared-interface or dependency changes

<!-- Did you touch src/shared/**, package.json, lockfile, tsconfig, vite/vitest config, .github/**?
     Only Agent A may merge those. If you need one, this PR should be titled "[integration] ..." and
     contain the smallest possible patch. Otherwise write "none". -->

## Known limitations / follow-ups

## Handoff updated?

- [ ] `docs/handoffs/<A|B|C>.md` updated with branch, commit, what works, what remains, test results
- [ ] (B/C) `docs/evidence/<codex|devin>.md` updated with what the tool actually did
